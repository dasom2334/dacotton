---
title: "실패한 빌드를 찾았는데 빌드가 없었다"
date: "2026-07-28"
project: "dolmagochi"
summary: "main은 바뀌었지만 새 Vercel 배포가 생성되지 않은 날."
source_notes: ["projects/dolmagochi/docs/sessions/2026-07-28-bedroom-scene-v3.md"]
review_status: "검수 대기"
series_order: 20
no: "20 / 24"
tldr: "새 배포가 아예 생기지 않은 문제를 훅으로 복구하고, 빌드 실패와 미생성을 구분하는 감시를 붙였다."
---

## main과 라이브가 다른데 배포는 0건

침실을 반영했는데 라이브 화면이 바뀌지 않았다. 이번에는 브라우저에 남은 구버전만의 문제가 아니었다. main에는 새 머지 커밋이 있었지만 서비스는 7월 23일 빌드에 머물러 있었고, 새 커밋의 Vercel 배포 기록은 아예 없었다.

## 빌드 이전 구간으로 원인 범위 축소

GitHub의 푸시와 CI 실행은 확인됐다. 배포 중지 여부, 프로덕션 브랜치, 빌드 무시 설정도 확인했다. 빌드가 시작됐다가 실패한 것이라면 살펴볼 실행 기록이 있어야 하는데 그 단계까지 가지 않았다. 조사 범위를 GitHub에서 Vercel로 이어지는 배포 시작 구간으로 좁혔다.

## 미생성일 때만 훅으로 복구

Deploy Hook으로 밀린 배포를 복구하고, main 푸시 이후 실제 배포가 나타나는지 검사하도록 했다. 빌드 실패와 배포 미생성을 나눠 후자일 때만 훅을 호출한다. 무작정 재배포하면 정상적으로 실패한 빌드도 반복 실행할 수 있기 때문이다.

## 관측 시점을 고려한 5분 대기

기존 배포 30건을 살펴보며 기다릴 시간도 정했다. 검사에 사용하는 배포 레코드는 준비가 끝난 뒤 나타나므로 이벤트 전달만 기다려서는 부족했다. 빌드 시간까지 포함해 5분을 기다리도록 했다.

## 같은 현상을 감지할 경로 확보

외부 서비스 내부의 고장 원인까지 알아낸 것은 아니다. 대신 앞으로 같은 현상이 생기면 감지하고 복구할 경로는 만들었다. 자세한 구분 조건은 [배포 미생성 오답노트](/mistakes/2026-07-28-dolmagochi-deployment-missing/)에 남겼다.
